voxels(100, 100) {
    // TODO
}

objects {
    highlight_bind(15);

    tex = texture("plasma");

    suzanne = spawn(
        "suzanne", mesh = object("suzanne"),
        phong = shader("phong", "phong"),
        phong_uniforms = [(4, tex), (5, tex)]
    );

    enable_highlight(phong, "highlight_map");

    transform(
        suzanne,
        pos = (0, 0.5, 0), rot = (0, 0, 0),
        scale = (0.5, 0.5, 0.5)
    );

    add_collider(suzanne, monkey = collider("suzanne_collider"));
}